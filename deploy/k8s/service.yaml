apiVersion: v1
kind: Service
metadata:
  name: social-agent
  namespace: social-agent
spec:
  selector:
    app: social-agent
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
